# 使い方

## 起動する

`PDF Studio を起動.bat` を**ダブルクリック**するだけです。

黒い画面が出て、少ししてからブラウザが開きます。それが PDF Studio です。

> **黒い画面は閉じないでください。** これが本体です。閉じると PDF Studio も止まります。
> 使い終わったら、この黒い画面を閉じてください。

デスクトップから起動したい場合は、`PDF Studio を起動.bat` を右クリック →
「ショートカットの作成」→ できたショートカットをデスクトップに移動してください。

## PDFを開く

ブラウザの画面に **PDFをドラッグ＆ドロップ**するか、左上の「開く」を押します。

パスワード付きのPDFなら、パスワードを聞かれます。

## 基本の操作

上のツールバーから道具を選んで、ページの上で操作します。

| やりたいこと | 手順 |
|---|---|
| 文字にマーカーを引く | 「ハイライト」→ ページ上の文字をドラッグして選択 |
| 文字を書き込む | 「テキスト」→ 書きたい場所をドラッグ → 文字を入力 |
| 手書きする | 「ペン」→ そのまま描く |
| 図形を描く | 「矩形」「円」「線」→ ドラッグ |
| 付箋を貼る | 「付箋」→ 貼りたい場所をクリック → 文字を入力 |
| 消す | 「選択」→ 消したいものをクリック → Delete キー |
| やり直す | Ctrl+Z（戻す）／ Ctrl+Y（やり直し） |

**色や太さを変えると、それが次に描くときの既定になります。** 毎回設定し直す必要はありません。

## 保存する

**自動で保存されます。** 画面右下に「保存済み」と出ていれば書き込み済みです。

すぐ保存したいときは Ctrl+S。

完成したPDFを取り出すときは、上の **「PDF書き出し」** を押してください。
ダウンロードフォルダに保存されます。

## よく使う機能の場所

上の「**注釈 ▾**」と「**文書 ▾**」に、細かい機能がまとまっています。

**注釈 ▾**
- 注釈一覧をPDFで書き出す（レビュー結果を渡すとき）
- CSV / Markdown で書き出す
- フラット化（注釈を焼き付けて編集できなくする）

**文書 ▾**
- ページの回転・削除・複製・結合
- **墨消し**（機密情報を本当に消す）
- **OCR**（スキャンしたPDFを検索できるようにする）
- 透かし・ヘッダー・フッター・ベイツ番号
- パスワードで保護して書き出す

## 右側のパネル

| タブ | 中身 |
|---|---|
| プロパティ | 選んだ注釈の色・文字・状態を変える |
| コメント | 全部の注釈の一覧。返信やステータスを付けられる |
| 数量 | 図面の計測と数量拾い（先に「縮尺を設定」が必要） |
| 設定 | 名前、ペンの設定、テーマ |

## 機密情報を消すとき — ここだけは注意

**黒い四角を上に置いても、下の文字は消えていません。** 選択してコピーすれば読めてしまいます。

本当に消すには:

1. 「墨消し」ツールで消したい場所を囲む（または「文書 ▾」→「検索して墨消しを指定」）
2. **「文書 ▾」→「墨消しを適用する」を実行する** ← これをやって初めて消えます
3. 続けて「非表示情報を削除」も実行すると、メタデータに残る痕跡まで消えます

## 困ったとき

| 症状 | 対処 |
|---|---|
| ブラウザが開かない | 黒い画面が出ていれば、ブラウザで `http://127.0.0.1:8000/` を開く |
| 「ポートが使用中」と出る | すでに起動しています。前の黒い画面を探すか、一度閉じてから起動し直す |
| 動作がおかしい | ブラウザで F5（再読み込み）。保存済みの内容は消えません |
| 操作を間違えた | ページ操作や墨消しの前は自動でバックアップされます。`data/work/<番号>/snapshots/` にあります |
