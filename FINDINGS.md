# 総点検で見つかった不具合と対応

全機能をブラウザ上で機械的に操作し、実PDFの中身と突き合わせて確認した記録。
「アプリ上では正しく見えるのに、保存すると違う」種類の問題を重点的に探した。

---

## 修正済み

### 1. Undo後のRedoが、ドラッグの最初の一歩しか復元しなかった

**症状** 図形をドラッグで移動 → Undo → Redo すると、移動量のごく一部しか戻らない。
リサイズ、不透明度スライダー、プロパティ欄への入力も同じ。

**原因** 連続ジェスチャを1つの履歴にまとめる仕組みが、履歴に「最初に届いたパッチ」だけを
保持していた。ドラッグ中の後続の更新は記録を更新せずにモデルを直接書き換えていたため、
Redo はその最初の1ステップだけを再適用していた。

**対応** 履歴を「パッチの再適用」から「変更前・変更後のスナップショット」方式に変更。
あわせて、同じジェスチャかどうかを一意のトークンで判定するようにした。以前は
「同じ図形を同じ種類で操作したか」で判定していたため、**別々の2回のドラッグが1つの履歴に
融合してしまう**副次的な不具合もあった（Undo 1回で2回分が戻ってしまう）。

### 2. 回転したページで、注釈が本文からずれた位置に描かれた

**症状** ページを90°回転すると、注釈が対応する文字から離れた場所に表示される。
自分で回転させた場合だけでなく、**もともと回転情報を持つPDF（横向きスキャンなど）を
開いただけでも**同じことが起きる。

**原因** 座標系が2つあり、境界で変換していなかった。

| | 座標系 |
|---|---|
| PyMuPDF（`annot.rect`・`search_for`・`get_text`）| ページの回転を無視した「元の向き」 |
| pdf.js の表示・canvas・テキスト層 | 回転を適用した「見たままの向き」 |

**対応** API の境界で変換するようにした（`backend/common.py` の `to_view` / `to_page`）。
外に出る座標はすべて「見たままの向き」、内側は「元の向き」に統一。注釈の読み書きだけでなく、
検索結果・本文ブロック・画像・フォーム欄・署名・読み上げ順序・トリミング範囲もすべて揃えた。

### 3. テキスト層の位置が本文とずれていた

**症状** 文字を選択してハイライトを引くと、実際の文字と少しずれた場所に付く。
回転ページでは大きくずれる。

**原因** ふたつ重なっていた。

- pdf.js はテキスト層の大きさを `round(down, …, var(--scale-round-x))` で指定するが、
  この CSS 変数を定義していなかったため指定全体が無効になり、テキスト層が親要素いっぱいに
  広がっていた。中の文字は％指定で配置されるので、全体が伸びた分だけ位置がずれる。
- pdf.js は各文字の大きさを `--font-height` `--scale-x` といった変数で表現するが、
  こちらで用意していた CSS がそれらを使っていなかったため、文字が既定サイズで並んでいた。
- 回転ページでは、テキスト層は「元の向き」で組まれるため、回転させて重ねる必要があった。

**対応** pdf.js 本体のスタイルシートに合わせた指定に差し替え、必要な CSS 変数を定義し、
回転時はテキスト層自体を回転させるようにした。

### 4. フラット化・全削除で、進捗が出ずサムネイルが古いままだった

**症状** フラット化は数秒かかることがあるが、その間なんの表示も出ないため固まったように見える。
終わったあともページ一覧のサムネイルは焼き付け前の見た目のままだった。

**原因** ほかのページ操作は共通の処理を通っていて、進捗表示とサムネイル再描画が入っていたが、
この2つだけ独自にサーバを呼んでいた。

**対応** 共通の処理に合流させた。サーバ側も、ほかのページ操作と同じ形の応答を返すようにした。

### 5. しおり・サムネイル・コメントをクリックしてもページが移動しないことがあった

**症状** クリックしても何も起きない。

**原因** 移動をなめらかスクロール（`behavior: 'smooth'`）だけに任せていた。
これを無効にしているブラウザや環境では、スクロールが一切行われない。

**対応** なめらかスクロールを試したうえで、動き出していなければ即座に移動するようにした。
「動きを減らす」設定の環境では最初から即座に移動する。
反応しないボタンより、急に飛ぶほうがまだよい。

### 6. 保存中に加えた編集が、保存されないまま残ることがあった

**症状** 注釈が多い文書（実測: 720件で1回の保存に約6秒）で、保存中にさらに編集すると、
その編集がファイルに書かれないまま「未保存の変更あり」で止まる。次に何か編集するまで
保存が再開されない。

**原因** 自動保存の時刻が来たとき、すでに保存が動いていると**何もせずに終了**していた。
再試行の予約をしていなかった。

**対応** 保存中なら、取りやめるのではなく次の保存を予約するようにした。あわせて、
送信した内容と現在の内容を突き合わせ、保存中に増減していれば「保存済み」にせず
もう一度書くようにした。

**副次的な改善** 自動保存の間隔を、直前の保存にかかった時間から決めるようにした。
毎回全注釈を書き直す方式なので、注釈の多い文書では4秒ごとだとほぼ書きっぱなしになる。

### 7. 文書を開き直すと、前の文書のページ監視が残っていた

画面外のページを描画しないための監視が、文書を切り替えても解除されていなかった。
表示には影響しないが、開くたびに参照が積み上がる。開始時に解除するようにし、
あわせて**ページの大きさが決まってから監視を始める**ようにした（大きさが決まる前は
全ページが同じ位置に重なっており、「画面内」の判定が正しく行えない）。

### 8. パスワード付きPDFを開くとサーバ内部エラーになった

**症状** 保護されたPDFを開こうとすると 500 エラー。「パスワードが必要です」とは表示されず、
入力する手段もなかった。つまり**保護されたPDFは一切扱えなかった。**

**原因** 暗号化されたままページを読もうとして例外になっていた。

**対応** パスワードを尋ねて開けるようにした。
- 保護された文書は 401 と「この文書はパスワードで保護されています」を返す
- 画面はそれを受けてパスワード入力を出し、入力後に再試行する
- 間違っていれば「パスワードが違います」と伝えて再入力できる
- オーナーパスワードでも開ける

解錠した作業用コピーは保護が外れた状態になる（そうしないと以降の操作が成立しない）ため、
開いたときにその旨を伝え、「パスワードで保護して書き出す」への導線を示すようにした。

---

## 調べた結果、問題ではなかったもの

- **ページ回転で注釈が内容から外れないか** — PyMuPDF 側は一貫しており、
  0°/90°/180°/270° のいずれでも、また4回転して一周させても、注釈は同じ文字の上に留まる。
- **セグメンテーション違反** — 検証スクリプトで発生したが、原因は
  `doc[0].annots()` のように Page を変数に保持せず注釈を触っていたこと。
  Page が解放された後に注釈を参照すると落ちる。アプリ本体は全箇所で Page を保持しており、
  同じ書き方は存在しなかった。
- **画面外ページの描画** — 一度「60ページ全部を描画している」と判定したが、これは
  測定の誤り。canvas は未描画でも既定値 300×150 を持つため、`width > 0` では判定できない。
  正しく測ると60ページ中3ページだけが描画されていた。スクロールすれば
  その周辺だけが追加で描画される。

---

## 確認済みの動作

以下はブラウザ上で実際に操作し、保存後の PDF の中身と突き合わせて確認した。

- 注釈19種すべての作成（テキストマークアップ・図形・手書き・付箋・スタンプ・墨消し）
- 選択・移動・リサイズ・マーキー選択・投げ縄選択・消しゴム
- Undo/Redo（追加・削除・移動・リサイズ、2回の別ジェスチャの分離）
- 複数ページへの注釈、ページをまたいだ保存
- 筆圧データの往復
- 回転（0/90/180/270°）での表示・作成・保存
- 書き出し4形式（注釈一覧PDF・XFDF・CSV・Markdown）と XFDF の読み戻し
- コメントの返信・ステータスが保存後も残ること
- しおり・サムネイル・コメント・検索結果からのページ移動
- ズーム率を変えても同じ位置に描かれること、幅／全体表示が正しく収まること
- フラット化・全削除（進捗表示、サムネイル更新、実ファイルへの反映）
- 素直でないファイルを開いたときの挙動:
  PDFでないファイル / 末尾が欠けた壊れたファイル / パスワード付き /
  白紙1ページ / ページ寸法と回転がばらばら / 120ページ

### 実測した性能（60ページ・注釈720件）

| | |
|---|---|
| 60ページの文書を開く | 画面近くの3ページだけ描画 |
| 全文検索（720件ヒット） | 約0.3秒 |
| 検索結果への一括ハイライト | 720件生成 |
| 720件の保存 | 約6秒（自動保存の間隔はこれに応じて自動で広がる） |
| コメント一覧 | 720件すべて表示 |
