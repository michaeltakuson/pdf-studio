# GitHubで公開する／ブラウザだけで動かす

質問された2点について、推測ではなく**実際に試した結果**をまとめます。

---

## 1. GitHubで公開できるか → できます。ただし条件があります

**必須条件: ソースコードを公開すること。** これは選択ではなく義務です。

理由は `LICENSE-NOTICE.md` に書いたとおりで、PDF処理に使っている PyMuPDF が
**AGPL-3.0** だからです。AGPL には「ネットワーク越しに使わせた相手にもソースを渡す」
という条項があり、これが他のオープンソースライセンスと決定的に違う点です。

| やりたいこと | 可否 |
|---|---|
| 自分のPCで使う | 問題なし。何の義務も生じません |
| GitHubの**公開**リポジトリに置く | できます。ソース公開の義務がそれで満たされます |
| GitHubの**非公開**リポジトリに置く | できます（他人に配らない限り義務は生じません） |
| ソースを公開せずにサーバに置いて他人に使わせる | **できません。** AGPL違反です |
| ソースを公開せずに配布・販売する | **できません。** 商用ライセンスの購入が必要です |

同梱済みのもの:

- `LICENSE` — AGPL-3.0 の全文（リポジトリのルートに置いたままにしてください）
- `LICENSE-NOTICE.md` — なぜこうなるかの説明と、同梱ソフトのライセンス一覧
- `frontend/vendor/pdfjs/LICENSE` — pdf.js の Apache-2.0。**削除しないでください**

### あなたにやっていただく作業

1. GitHub でリポジトリを作る（公開・非公開はどちらでも可）
2. このフォルダを push する

`.gitignore` は用意済みで、作業ファイル（`data/work/`）や生成物は除外されます。
`frontend/vendor/pdfjs/`（約15MB）は**含めてください**。これが無いとアプリが動きません。

コマンドが必要なら、こちらで実行できます。GitHub のアカウント作成とリポジトリ作成だけは
ブラウザでの操作になるので、そこはお願いすることになります。

---

## 2. ブラウザだけで動かせるか → 動きます（実証済み）

いま Python サーバが必要なのは、PDF の読み書きを PyMuPDF が担っているからです。
その PyMuPDF に **WebAssembly 版**があり、ブラウザの中で直接動きます。

### 実際に試した結果

ブラウザ上で Pyodide（ブラウザ用 Python）を読み込み、PyMuPDF の WASM 版を入れて
動かしたところ、次がすべて成功しました。

```
PyMuPDF 1.28.2: Python bindings for the MuPDF 1.28.2 library.
  PDFを新規作成            → 成功
  文字を書き込む            → 成功
  ハイライト注釈を付ける      → 成功
  保存して読み直す          → 成功（注釈も残っている）
  日本語フォント            → 利用可能（Droid Sans Fallback）
```

所要時間の内訳（初回のみ。2回目以降はブラウザのキャッシュが効きます）:

| | |
|---|---|
| Python実行環境の起動 | 約11秒 |
| PyMuPDF の読み込み（17.5MB） | 約7秒 |
| 使えるようになるまで | 合計 約30秒 |

### 現時点での引っかかり

PyMuPDF の WASM 版が想定している実行環境の世代と、いま配布されている Pyodide の世代が
ずれています。今回は**ファイル名の世代表記を合わせる**ことで動きました。中身のプログラムは
そのままで問題なく動作しています。

これは時期が来れば自然に解消する類のものですが、いま作るなら、こちらで用意した
ファイルを同梱する形にすれば確実に動かせます。

### この方式にした場合の変化

**良くなること**

- サーバの起動が不要。`index.html` を開くだけ
- GitHub Pages に置けば **URL でアクセスできて、費用はゼロ**
- PDF がPCの外に出ない（すべてブラウザ内で処理されるため、いまより安全）

**悪くなること／できなくなること**

| | |
|---|---|
| 初回の待ち時間 | 約30秒、通信量 約30MB。2回目以降は速い |
| **OCR** | Tesseract は別のプログラムなので動きません。JavaScript版（tesseract.js）に置き換える作業が必要です |
| ファイルの保存先 | サーバ上の作業ファイルが使えないため、ブラウザ内保存＋明示的なダウンロードに変わります |
| 自動スナップショット | 同上。作り直しが必要です |

### 作業量の見積もり

PDF処理の部分（`backend/` 配下）はほぼそのまま流用できます。Python のコードが
ブラウザ内で動くためです。書き換えが必要なのは、いま HTTP で通信している部分を
関数呼び出しに置き換えるところと、ファイルの保存まわり、そして OCR です。

小さくない作業ですが、**ゼロから作り直しにはなりません。**

---

## どちらを選ぶか

いますぐ確実に使いたいなら、**現状のまま（サーバ版）で GitHub に公開**するのが堅実です。
機能はすべて揃っていて、テストも通っています。

「URLを開くだけで誰でも使える」形にしたいなら、ブラウザ版への移行になります。
実現可能であることは今回確認できました。やるかどうか、指示をいただければ着手します。
